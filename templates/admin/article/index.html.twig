{% extends "base.html.twig" %}

{% block stylesheets %}
<link rel="stylesheet" href={{asset('css/style.css')}}>
<link rel="stylesheet" href={{asset('css/article.css')}}>
{% endblock %}

{% block title %}Articles Page{% endblock %}

{% block body %}
<h1>page article</h1>


<table class='table table-striped table-info'>

<tr>
<th>Nom</th>
<th>Description</th>
<th>Date de création</th>
<th>Modifier</th>
<th>Supprimer</th>

</tr>
 <a href={{ path('admin_article_create')}} class="btn btn-success m-2 ">Ajouter</a>

{% for candy in candies %}

   <tr>

    <td>{{candy.name}}</td>
	 <td>{{candy.slug}}</td>
	 <td>{{candy.createAt |date('d /m /Y')}}</td>

      <td>
    <a href={{ path('admin_article_update', { id: candy.id})}} class="btn btn-primary">Modifier</a>
    </td>

     <td>
    <a href={{ path('admin_article_delete', { id: candy.id})}} class="btn btn-danger">Supprimer</a>
    </td>
   </tr>

{% endfor %}

</table>
{% endblock %}